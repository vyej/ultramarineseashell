# Site settings
# These are used to personalize your new site. If you look in the HTML files,
# you will see them accessed via {{ site.title }}, {{ site.email }}, and so on.
# You can create any custom variable you would like, and they will be accessible
# in the templates via {{ site.myvariable }}.
title: Stands or Falls
email:
description: > 記住更美好而不是更丑惡的事，這無疑是高貴的，也更正義，更虔敬，更令人愉快。 --色諾芬《遠征記·卷五》 
baseurl: "/ultramarineseashell" # the subpath of your site, e.g. /blog
url: "https://vyej.github.io" # the base hostname & protocol for your site
github_username:
instagram_username:
twitter_username:

# Build settings
markdown: kramdown

plugins:
  - jekyll-paginate

paginate: 5
